{
  "name": "@realestate-radar/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/generate-metadata.mjs",
    "dev": "astro dev",
    "prebuild": "node ../scripts/generate-metadata.mjs --strict",
    "build": "astro build",
    "preview": "astro preview",
    "prelint": "node ../scripts/generate-metadata.mjs",
    "lint": "astro check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "maplibre-gl": "^5.4.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9"
  }
}
